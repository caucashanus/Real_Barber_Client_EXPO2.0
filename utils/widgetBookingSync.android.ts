import type { Booking } from '@/api/bookings';

/** Android — home screen widgety jsou zatím jen na iOS. */
export async function syncBookingWidgetFromBookings(_bookings: Booking[]): Promise<void> {}
